# 🚀 Быстрая публикация в npm

## Команды для копирования

```bash
# 1. Войти в npm (если ещё не залогинены)
npm login

# 2. Проверить, что имя пакета свободно
npm view trading-vue3-js

# 3. Собрать библиотеку
npm run build

# 4. Проверить, что будет опубликовано (dry run)
npm pack --dry-run

# 5. Опубликовать!
npm publish

# Если нужен scoped пакет (с @username):
npm publish --access public
```

## Чеклист перед публикацией

- [ ] ✅ Код собирается без ошибок (`npm run build`)
- [ ] ✅ Версия обновлена в package.json (текущая: 1.0.4)
- [ ] ✅ README.md актуален
- [ ] ✅ Залогинены в npm (`npm whoami`)
- [ ] ✅ Файл dist/ создан и содержит:
  - `trading-vue.es.js`
  - `trading-vue.umd.js`
  - `style.css`

## После публикации

1. ✅ Проверить на npmjs.com: https://www.npmjs.com/package/trading-vue3-js
2. ✅ Создать git tag:
   ```bash
   git tag v1.0.4
   git push origin v1.0.4
   ```
3. ✅ Создать GitHub Release

## Обновление версии (следующие релизы)

```bash
# Патч версия (1.0.4 → 1.0.5) - исправления
npm version patch

# Минор версия (1.0.4 → 1.1.0) - новые функции
npm version minor

# Мажор версия (1.0.4 → 2.0.0) - breaking changes
npm version major

# Опубликовать
npm publish
```

## Решение проблем

### Имя занято
Измените name в package.json:
```json
"name": "@your-username/trading-vue3-js"
```

### Нет прав на публикацию
```bash
npm login
npm whoami
```

### Ошибка сборки
```bash
rm -rf node_modules dist
npm install
npm run build
```

## Полная документация

См. [NPM_PUBLISH_GUIDE.md](./NPM_PUBLISH_GUIDE.md)
