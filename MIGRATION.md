# Миграция на Vite

Проект успешно мигрирован с Webpack на Vite. Основные изменения:

## Изменения в структуре

1. **Конфигурация**: Создан `vite.config.js` вместо webpack конфигураций
2. **HTML**: `index.html` перенесён в корень проекта (требование Vite)
3. **Web Workers**: Изменён способ импорта на `import Worker from './worker.js?worker'`
4. **Переменные окружения**: `MOB_DEBUG` заменён на `import.meta.env.VITE_MOB_DEBUG`

## Скрипты npm

- `npm run dev` - запуск dev сервера (раньше было webpack serve)
- `npm run build` - сборка проекта для production
- `npm run preview` - предпросмотр production сборки

## Преимущества Vite

- ⚡️ Мгновенный холодный старт
- ⚡️ Мгновенный HMR (Hot Module Replacement)
- 🛠️ Богатый набор функций из коробки
- 📦 Оптимизированная сборка на основе Rollup
- 🔩 Универсальные плагины
- 🔑 Полная поддержка TypeScript

## Web Workers

Web Workers теперь импортируются с суффиксом `?worker`:

```javascript
import Worker from './worker.js?worker'
const worker = new Worker()
```

## Переменные окружения

Создайте файл `.env` (на основе `.env.example`) для локальных переменных:

```
VITE_MOB_DEBUG=true
```

В коде доступ через `import.meta.env.VITE_*`

## Устаревшие файлы

Следующие файлы больше не используются (можно удалить):
- `webpack/` - все webpack конфигурации
- `babel.config.js` - Vite использует esbuild
- `src/index.html` - перемещён в корень

## Дополнительная информация

- [Vite Documentation](https://vitejs.dev/)
- [Vite Features](https://vitejs.dev/guide/features.html)
- [Vite Configuration](https://vitejs.dev/config/)
